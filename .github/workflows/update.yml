name: Update Bakery Jin Menu Daily

on:
  schedule:
    - cron: "0 0 * * *"  # 매일 00:00 UTC = 한국시간 오전 9시
  workflow_dispatch:     # 수동 실행 버튼도 생성

permissions:
  contents: write        # README 자동 커밋/푸시에 필요

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Py
